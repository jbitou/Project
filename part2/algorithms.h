#include "silhouette.h"

centroid *CLARA(hash_table, ghashp *, pinfo, void *, int);
pcluster IAU1(hash_table *, ghashp *, pinfo, void *, int, int, int);
pcluster IAU2(hash_table *, ghashp *, pinfo, void *, int, int, int);
